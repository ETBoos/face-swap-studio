# FaceSwap Studio — 产品规格（剧组销售用简版）

**产品名**：FaceSwap Studio（剧组换脸预览工作站）  
**版本**：0.1.0（产品壳 MVP）  
**日期**：2026-09  

## 主场景（产品定稿）

**本机实时换脸 → 微信 PC（虚拟摄像头）出站 → 对方手机微信接听。**

成功标准：对方手机看到换脸画面，且嘴型/身份过 P0 像真度验收。  
关键卡点：微信 PC 能否稳定选中虚拟摄像头（真机专项测试；不过则保持实验并钉版本/驱动）。

虚拟摄像头为**主路径**（非纯增值）；排期：真机一到先测选摄像头，再冲像真度样片。


> **售前口径**：目标场景可以是微信 PC 出站，但未真机测通前一律「实验支持」；唯一确定的是本机换脸引擎链路，不是微信兼容。

## 桌面通话兼容（产品定档）

| 级别 | 平台 | 路径 |
|------|------|------|
| **目标正式支持**（待 `REALTIME_ACCEPTANCE.md` + `COMPAT_MATRIX.md` 真机证据） | WhatsApp Desktop（Win） | 换脸画面 → OBS → **Media Foundation 虚拟摄像头** → WhatsApp 选摄像头；无证据前禁止写「已正式支持」 |
| **实验支持（未测通禁止写已支持）** | 微信 PC / 微信海外 PC | 同上；兼容性取决于腾讯客户端版本；**真机选中虚拟摄像头测试通过前不得对外称已支持** |
| **不做** | 手机微信 / 手机 WhatsApp | 系统不开放第三方虚拟摄像头 |

对外话术：「桌面视频通话：WhatsApp 优先，微信实验」。禁止写「支持微信 WhatsApp」全称通吃。

**档位**：虚拟摄像头输出仅 **Pro / Studio**；Starter 仅预览窗。虚拟摄像头（L1→L3）= 主场景出站路径，与换脸引擎同属 P0 主链路。

## 对外话术（产品定稿）

**正确表述**：「桌面端：虚拟摄像头 / OBS 可用的通话与直播」——本地双模式像真度。

**禁止表述**：「支持全部社交媒体」「原生对接所有 App」。

说明：换脸输出本质是虚拟摄像头/OBS 推流，对方软件把它当普通摄像头。Zoom / Teams / Discord / OBS→Twitch·YouTube 等可选摄像头或推流的桌面场景可覆盖；多数手机端 App 不在本期。虚拟摄像头（L1→L3）= **主场景出站路径**，与换脸引擎同属 P0 主链路。

## 1. 一句话

给影视剧组用的 **授权素材 + 实时预览** 工作站：管项目、管授权、出参考帧；换脸能力通过接入成熟开源实时管线（DeepFaceLive / InsightFace）交付，而不是自研新模型。

## 2. 目标用户与场景

- **用户**：摄影指导助理、数字妆容/预览岗、后期预览、制片指定的授权操作员  
- **场景**：棚内/现场用摄像头看“换脸后大概长什么样”，导出参考帧给导演/化妆/VFX 沟通  
- **非目标**：互联网娱乐换脸 App、未授权肖像玩法、自动生成成片最终镜头

## 3. 功能清单

### 3.1 本版本已提供（壳）

| 功能 | 说明 |
|------|------|
| 项目管理 | 本地项目目录、元数据、素材与导出子目录 |
| 授权素材导入 | 图片导入到项目；需勾选授权清单 |
| 授权清单 UI | 合同级流程辅助（书面授权仍由剧组法务负责） |
| 摄像头预览占位 | 占位引擎：摄像头或合成画面 + 合规横幅 |
| 开始/停止预览 | 控制占位管线 |
| 导出参考帧 | 写入项目 `exports/`，可选水印 |
| 设置 | 分辨率、摄像头索引、GPU 设备备注、引擎选择 |
| 使用日志 | 本地 JSONL 审计桩 |
| Windows 安装脚本 | `setup-win.bat` / `start-win.bat` |

### 3.2 集成后交付（客户 GPU 机）

| 功能 | 说明 |
|------|------|
| 实时换脸预览 | DeepFaceLive（或 InsightFace 实时方案）适配器 |
| GPU 加速 | RTX 4080/4090，设备字符串可配 |
| 多素材切换 | 项目内授权脸切换（视引擎能力） |

### 3.3 明确不做（MVP）

- 训练新 SOTA 换脸模型  
- 云端人脸库 / 陌生人脸检索  
- 绕过授权的批量爬取或“一键网红脸”

## 4. 系统要求（销售机）

- Windows 10/11 + NVIDIA **RTX 4080 或 4090**  
- 32 GB RAM 建议、SSD  
- Python 3.11+（安装脚本处理依赖）  
- 授权摄像头/采集设备  

## 5. 合规与销售话术边界

- UI 固定横幅：**仅限授权影视用途**  
- 销售合同建议条款：素材授权义务在客户侧；软件提供清单、水印、日志辅助  
- **集成验收前**不得宣传“已具备影院级实时换脸”；应表述为“预览工作站 + 待现场集成引擎”  
- 开源引擎许可证需单独评审后再决定捆绑或“客户自备引擎”

## 6. 交付物形态

1. 本仓库源码 / 压缩包（`face-swap-studio-transfer.tar.gz`）  
2. Windows 一键安装与启动脚本  
3. 引擎适配接口文档（`engines/deepfacelive_stub.py`）  
4. 现场：在客户 4090 机器上完成 DFL 接入与验收（另行工时）

## 7. 路线图（简）

1. **0.1** 产品壳（当前）  
2. **0.2** DeepFaceLive worker IPC + 真预览验收  
3. **0.3** 多机位、LUT/监看友好导出、加固审计日志  
4. **1.0** 剧组试点包（授权模板 + 培训 + 维保）

## 8. 风险摘要

| 风险 | 缓解 |
|------|------|
| 未授权滥用 | 合同 + UI 清单 + 水印 + 日志；拒绝无授权部署 |
| 开源许可证 | 法务评审；可改为客户自备 DFL |
| 性能预期 | 以现场 4080/4090 实测 FPS 为准，不写死营销数字 |
| 与成片品质落差 | 定位为“预览沟通工具”，非最终 VFX 成片 |


## 3.3 双模式（2026-09 更新）

| 模式 | 后端 | 用户动作 |
|------|------|----------|
| 简易 | FaceFusion | 导入目标脸，简单配置即可预览 |
| 顶级 | DeepFaceLive | 导入训练好的 `.dfm` 后实时预览 |

壳负责模式切换；真推理分别由 FaceFusion / DeepFaceLive 适配器在 GPU 机接线。


## 出站路径优先级（主链路，非 P2）

1. L1：OBS + DroidCam Virtual Output（驱动+插件）— 见 `docs/L1_VIRTUAL_CAM.md`
2. L2：钉版本矩阵
3. L3：采集卡环回 — 见 `docs/L3_CAPTURE_CARD_BEGINNER.md`（扩展屏优先；一分二≠扩展桌面）

验收：`docs/REALTIME_ACCEPTANCE.md`  
引擎锁定：`docs/ENGINE_VERSION_PIN.md`

## 微信 PC 出站技术档位（L1→L2→L3）

引擎（FaceFusion / DeepFaceLive）不改为微信定制；只解决「出站设备被微信认成摄像头」。

| 档 | 做法 | 验收 |
|----|------|------|
| **L1** | 换脸画面 → OBS → **Media Foundation 虚拟摄像头**（如 DroidCam Virtual Output）→ 微信 PC 选用 | 通 = **主路径转正** |
| **L2** | 钉死微信版本 + MF 驱动名 + 分辨率/FPS 与 OBS 一致；不通换 MF 实现，不换引擎 | 矩阵记录版本组合 |
| **L3** | 换脸窗口/全屏 → HDMI/采集卡环回 → 系统出现真实 UVC 摄像头 → 微信当普通摄像头选 | 通则产品可用，须标「需采集卡环回」，进 Studio 硬件清单 |

真机顺序：L1 → 不通上 L2 → 再不通上 L3。禁止只用 OBS DirectShow VirtualCam 作为唯一方案（腾讯系经常不认）。

**交付分档**：软件标配只承诺 L1/L2 软件路径；L3「HDMI 环出 + HDMI→USB UVC 采集卡（建议 1080p60）」为 **Studio 可选件**（套装加售或客户自备），不随软件标配。

